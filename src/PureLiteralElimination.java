import java.util.*;

public class PureLiteralElimination {
    private static HashMap<Character, Boolean> pureLiteralElimination(ArrayList<ArrayList<Character>> clauses){
        // Look for all the pure literals
        // Assign each pure literal truth values

        // For each pure literal found
            // For each clause in the formula
                // remove the clause if it contains the pure literal
    }
}
