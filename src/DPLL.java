import java.util.HashMap;
public class DPLL {
    private static HashMap<Character, Boolean> dPLL(String formula){
        HashMap<Character, Boolean> pureLiteralsTruthValues = new HashMap<>();

        // Formula

        // Unit prop
        // Pure literal elim (Need to keep track of pure literals being removed)
        // If clause empty SAT, show pure literals truth values
        // If clause has one empty clause NOT SAT

        // Now do that again but set one of the literals to true or false
    }

    public static void main(String[] args) {
        String formula = "(avb)^(avB)";


    }

}
